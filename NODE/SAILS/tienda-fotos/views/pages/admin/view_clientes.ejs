<section class="container my-3">
    <% flash.get('mensaje').forEach( function (msg){%>
      <div class="alert alert-primary" role="alert">
          <%=msg%>
      </div>
    <% }); %>
    <article class="row border-bottom mb-3">
      <h1 class="col-12">ADMIN - Clientes</h1>
    </article>
    <article class="row">
        <table class="table table-hover">
            <thead class="table-dark">
              <tr>
                <th scope="col">#</th>
                <th scope="col">nombre</th>
                <th scope="col">email</th>
                <th scope="col">acciones</th>
              </tr>
            </thead>
            <tbody>
              <%clientes.forEach(cliente =>{%>
              <tr>
                <th scope="row"><%=cliente.id%></th>
                <td><%=cliente.nombre%></td>
                <td><%=cliente.email%></td>
                <td class="text-end"><a class="btn btn-primary btn-sm" href="/admin/ordenes-cliente/<%=cliente.id%>">Ver ordenes</a> <a class="btn btn-danger btn-sm ms-2" href="/admin/ordenes-cliente/<%=cliente.id%>">Desactivar cliente</a></td>
              </tr>
              <%})%>
            </tbody>
        </table>
    </article>
</section>